# TODO 06022026
> Consolidated from `todo.md`, `review.md`, `Proposal.md`, `PLAN.md`,
> `FEATURES_PLAN.md`, `AUDIT.md`, and `README.md`.
>
> Items below are only included when they still appear unfinished or remain a
> useful follow-up after checking the current code. Completed roadmap items such
> as mobs, mob JSON loading, block/parry, towns/NPC shops, Slime Lair dungeons,
> minimap, particles, chat, creative/admin commands, death/respawn, durability,
> weather, character customization, and the dispatcher-based TCP state handler
> were not carried forward except where they expose a remaining gap.

---

## Priority 1 - Security / Correctness

- [ ] **Fix missing durability drain coverage**
  - Current status: wands do not lose durability, and neither do shields, pauldrons, or gloves.
  - Target files to audit first: `server/network/combat.py`, `server/network/projectiles.py`, `server/mobs/mob_manager.py`, and any item-consume / block / hit-response durability helpers in `server/item_data.py`.
  - Desired outcome: weapon, offhand, and armor-slot durability rules are explicit and consistent instead of depending on which combat path happened to fire.

- [x] **Add real session authentication for movement packets**
  - Completed 2026-06-02: `server/session_auth.py` now issues per-login tokens; UDP assign returns the token; UDP movement/attack packets and TCP world/state handshakes require it.
  - Verified manually: client join/logout path still works after token plumbing.

- [x] **Add stronger attack plausibility checks**
  - Completed 2026-06-02: `server/network/combat.py` keeps a server-side last-attack timestamp and rejects repeated swings inside the cooldown, while retaining position tolerance, range, cone, and LOS checks.

- [x] **Audit remaining direct `data[...]` packet reads**
  - Completed 2026-06-02: hardened `server/network/tcp_state_handlers_v2.py` dispatcher and high-risk handlers with non-dict rejection, type/range checks, safe station lists, safe chunk-forget filtering, appearance value filtering, slot bounds, and spell position validation.
  - Follow-up: future handlers should use the same helper pattern as they are added.

- [x] **Add tests for death/respawn edge cases**
  - Completed 2026-06-02: added tests covering delayed respawn, bed spawn, origin fallback, and minimum respawn HP in `tests/test_server_logic.py`.
  - Note: full unittest execution was blocked locally by Python/dependency mismatch, but changed files compile and focused dependency-light checks passed.

---

## Priority 2 - Data-Driven Modularization

- [ ] **Resource node registry JSON**
  - Current status: `server/world/resource_nodes.py` hardcodes `NODE_TYPES`, `TOOL_ITEMS`, `TOOL_DAMAGE`, and pick tier ranks.
  - Client duplicates related data in `client/input/controls.py`, `client/input/controls_movement_v2.py`, `client/rendering/display.py`, and `client/networking/handlers.py`.
  - Target: `data/resource_nodes/*.json` plus a loader similar to `server/mobs/mob_data.py`.
  - Suggested fields: `hp`, `yields`, `tool`, `respawn`, `density`, `spawn_biomes`, `min_dist`, `permanent`, `seed_drop`, `collision`, `render_scale`, `y_sorted`.

- [x] **Tool tier and gathering rules registry**
  - Completed 2026-06-02: tool satisfaction tiers, mining damage, and mining tier ranks now load from `data/tools.json` on both server and client.
  - Follow-up: the broader `NODE_TYPES` resource-node registry is still separate and remains the next larger extraction.

- [ ] **Placeable/station/farming registry JSON**
  - Current status: `server/game_state/placed_objects.py` hardcodes `PLACEABLE_ITEMS`, `GROW_TIMES`, `GROWS_INTO`, floor types, and solid types; client repeats placeable ids in `client/input/controls.py` and `client/rendering/display.py`.
  - Target: `data/placeables/*.json`.
  - Suggested fields: `item_id`, `type`, `solid`, `layer`, `station`, `interactions`, `grows_into`, `grow_time`, `light_radius`, `default_state`.

- [x] **NPC shop JSON**
  - Completed 2026-06-02: shop stock now loads from `data/shops/{npc_type}.json` via `server/world/npc_shops.py`.
  - Verified manually: shop open/buy/sell paths worked after the JSON move.
  - Follow-up: blacksmith gloves entry was corrected from invalid item `3278` to `3572`; verify that stock in-game on next test pass.

- [x] **Part combiner mold registry**
  - Completed 2026-06-02: mold output, display name, armor flag, and required primary part slot now load from `data/molds.json` on both server and client.
  - Verified manually: combiner popup opened and successfully created an item after the registry move.

- [x] **Repair cost registry**
  - Completed 2026-06-02: repair cost metadata now lives in `data/repair.json`; server and client both load the same rules.
  - Verified manually: repair flow still worked after the registry move.
  - Follow-up bug: check repair recipe coverage for newer weapon families; `Crystal Katana` (`1855`) is currently not repairable because no matching rule exists in `data/repair.json`.

- [x] **Gem trait/effect registry**
  - Completed 2026-06-02: gem trait, display color, and effect key now load from `data/gems.json` for both embedder UI and server combat logic.
  - Bug fix included: the Light gem now maps to lifesteal correctly instead of being lost to the old `Light` vs `Life` mismatch.

- [x] **Spell/projectile registry**
  - Completed 2026-06-02: projectile speed/range/hit radius/cooldown plus per-element damage/effect/visual tuning now load from `data/projectiles.json`.
  - Server projectile logic and client projectile rendering both read the shared registry.
  - Bug fix included: nature projectile lifesteal now caps against effective max HP instead of bare base HP.

- [x] **Status effect registry**
  - Completed 2026-06-02: poison, burn, and slow metadata now load from `data/status_effects.json`.
  - Server status application/ticking and client status overlays/movement slowdown now use the shared registry.
  - Follow-up: manual in-game validation still needed for wand projectile status application and poison/slow visual feel.

- [ ] **Shared biome/cliff constants**
  - Current status: biome/cliff ids are duplicated across server world generation, mob data loading, and client config.
  - Target: `data/world_types.json` or `data/biomes.json` that both client and server load.

- [x] **Shared quality/progression config**
  - Completed 2026-06-02: craft quality tiers, loot roll tiers, sell multipliers, stat labels, stat names, and stat upgrade amounts now load from `data/progression.json`.
  - Server crafting/item roll logic and client inventory/crafting/stat screens now read from the shared config.

---

## Priority 3 - Architecture / Code Size

- [ ] **Split `client/input/controls.py` by subsystem**
  - Current status: still a large mixed input file handling chat, shop, inventory drag/drop, placement, station interaction, and UI toggles.
  - Target modules: `controls_inventory.py`, `controls_building.py`, `controls_interaction.py`, `controls_chat.py`, with movement already separated in `controls_movement_v2.py`.
  - Audit note 2026-06-03: this file is still the biggest client-side friction point for maintenance and bug isolation.

- [ ] **Split `client/rendering/item_art.py` into domain files**
  - Current status: `item_art.py` remains a very large dispatcher plus all drawing functions.
  - Target modules: materials, tools, weapons, armor, placeables, gems/food, and a thin dispatcher.

- [ ] **Move more item image callers to `rendering.cache.get_item_surface()`**
  - Current status: `combiner.py`, `embedder.py`, and `repair.py` use the centralized cache, but inventory/crafting/chest/shop still route through `inventory._get_item_image()`.
  - Target: make `rendering.cache` the shared surface API for all item UI rendering.

- [ ] **Extract mob manager behavior into smaller modules**
  - Current status: mobs are data-driven for stats/spawn/sprite config, but `server/mobs/mob_manager.py` still owns one large state machine and many behavior branches.
  - Target: keep one lifecycle manager, but move behavior handlers/effects/spawn helpers into focused modules or behavior strategy functions.

- [ ] **Separate mutable client state from constants**
  - Current status: `client/config.py` contains both constants and live session state.
  - Target: split into `client/constants.py` and `client/state/session.py`, or introduce a state object while keeping compatibility wrappers.

- [ ] **Move `RemotePlayer` out of networking handler**
  - Current status: `RemotePlayer` lives in `client/networking/handlers.py`.
  - Target: `client/state/remote_player.py` or `client/entities/remote_player.py`.
  - Audit note 2026-06-03: `client/networking/handlers.py` currently mixes transport code, world-node cache management, and entity interpolation.

- [ ] **Move equipment layer order to data**
  - Current status: `client/rendering/equipment_layers.py` has `_LAYER_ORDER` in code.
  - Target: manifest JSON that also supports future LPC asset packs.

- [ ] **Remove duplicate helper definitions / shadowed code**
  - Current status: `client/rendering/display.py` defines `draw_placed_objects()` twice.
  - Target: keep one source of truth per helper and trim dead/overwritten definitions during cleanup passes.

---

## Priority 4 - Gameplay Systems Still Pending

- [ ] **Combo chain**
  - Current status: no `combo_count` implementation found.
  - Target: attacks within a short window increase combo count up to 4, with server-authoritative damage multiplier and a fading HUD counter.

- [ ] **Bow / ranged weapon attacks**
  - Current status: wand/projectile spells exist, but the planned bow/arrow charge system was not found.
  - Target: ranged slot type, arrows as ammo, charge/release input, server projectile spawn, client projectile render.

- [ ] **Mana and expanded magic**
  - Current status: wand projectiles exist; no mana resource was found.
  - Target: mana stat/bar, regen, spell costs, staff/wand spell selection, and fire/ice/etc. data-driven spell definitions.

- [ ] **Player-to-player trading**
  - Current status: NPC shops exist; no player trade flow found.
  - Target: request/accept flow, escrowed trade slots, two-player confirm, server validation.

- [ ] **Party system**
  - Current status: no party invite/share system found.
  - Target: party invites, max size, shared XP near party members, party minimap markers, dungeon vote hooks.

- [ ] **PvP and safe zones**
  - Current status: player-vs-player damage exists through combat, but no safe-zone or opt-in PvP policy was found.
  - Target: town safe zones, PvP opt-in/out, friendly-fire rules, PvP death rewards/penalties.

- [ ] **Skill tree**
  - Current status: stat points exist, but no skill tree persistence/UI found.
  - Target: skill points per level, skill tree save field, client panel, combat/crafting modifiers.

- [ ] **Class selection**
  - Current status: character customization exists, but no class choice found.
  - Target: one-time class selection persisted in save data with base stat and skill-tree effects.

- [ ] **Achievements**
  - Current status: no achievement module found.
  - Target: server-side achievement conditions, persistence, and client toast notification.

- [ ] **Loot rarity overhaul**
  - Current status: quality tiers exist, but no Legendary/Set tier or set-bonus system found.
  - Target: add Legendary and Set definitions, set membership in item data, equipped-set bonus calculation.

- [ ] **Deeper dungeon instancing**
  - Current status: deterministic Slime Lair structures and Slime King trigger are implemented.
  - Remaining roadmap: true dungeon instances, multi-room generation, room-clear state, boss-room locks, and per-player dungeon chest loot.

- [ ] **Mob persistence**
  - Current status: no `mob_state.json` save/load path found.
  - Target: save active mobs on clean shutdown, reload non-stale mob state on startup.

---

## Priority 5 - Client UX / Polish

- [ ] **Fix character appearance menu input/hitboxes**
  - Current status: the Control+C character appearance menu behaves inconsistently and only works once per game load.
  - Bug: visible buttons do not line up with the actual clickable hitboxes.
  - Target: make the menu reopen reliably during the same session, align button rects with rendered controls, and add a quick regression check for open/close/reopen plus click targeting.

- [ ] **Settings menu**
  - Current status: launcher options and controls rebinding exist, but no full settings menu or `client/settings.json` was found.
  - Target: graphics/audio/controls/network tabs, resolution/fullscreen, audio sliders, persistence.

- [ ] **Sound effects**
  - Current status: no `client/audio` directory or mixer-based sound system found.
  - Target: audio manager plus sounds for footstep, swing, hit, mob death, pickup, UI click, craft, level-up.

- [ ] **Background music**
  - Current status: no music system found.
  - Target: biome/town/dungeon track list, streaming music, crossfade on context changes.

- [ ] **Animated tiles**
  - Current status: no water/grass tile animation system found.
  - Target: water ripple frames, optional grass sway, future lava animation.

- [ ] **Biome blending and elevation visual polish**
  - Current status: world tiles already carry biome/elevation-style data, cliffs exist, and the minimap/info overlay reads elevation.
  - Remaining roadmap from `# PERFORMANCE ROADMAP.txt`: smoother biome transitions and stronger visual depth from elevation.
  - Target: blend tile edges between nearby biomes where appropriate and add subtle elevation shading/overlays without hurting chunk render performance.

- [ ] **NPC sprite assets**
  - Current status: NPCs render procedurally in `client/rendering/npcs.py`.
  - Target: optional asset-backed idle sprites for merchant, blacksmith, healer, innkeeper.

- [ ] **Mob sprite sheet expansion**
  - Current status: mob renderer loads JSON sprite configs for some mobs and procedural sprites for others.
  - Target: 4-directional walk and attack sheets for every mob where assets are available.

- [ ] **Responsive UI panels**
  - Current status: some inventory/crafting/shop-style panels still use fixed dimensions.
  - Target: all panels clamp to current window size and avoid clipping at non-standard resolutions.

- [ ] **Improve fullscreen switching**
  - Current status: `client/rendering/display.py` still calls `pygame.display.quit()` / `pygame.display.init()`.
  - Target: use `pygame.display.set_mode()` flag changes directly where possible.

- [ ] **Minimap performance pass**
  - Current status: minimap works and persists visited chunks.
  - Current audit note: `client/client.py` copies `full_world_data.items()` while the map is open, and `client/rendering/display.py` rebuilds biome counts from all known tiles.
  - Target: cache minimap quarter-chunk colors as chunks arrive, then redraw only the player dot/current viewport instead of rebuilding from all explored tiles.

- [ ] **Client-side world object lookup indexes**
  - Current status: placement blocking and nearby station checks scan `config.world_nodes` and `config.placed_objects`.
  - Target: maintain client mirrors such as `node_by_tile`, `object_by_tile`, `floor_by_tile`, and `stations_by_chunk` as state packets arrive.
  - Benefit: placement ghost checks, station detection, and pickup/interaction targeting become O(1) or small-neighborhood lookups.
  - Audit note 2026-06-03: this scan pattern still appears in `client/client.py`, `client/input/controls.py`, and `client/input/controls_movement_v2.py`.

- [ ] **Cache projectile glow surfaces**
  - Current status: `client/rendering/display.py` creates a new translucent glow surface for every projectile every frame.
  - Target: prebuild/cache projectile glow surfaces by element/color and reuse them during rendering.

- [ ] **In-game diagnostics overlay / `/perf` command**
  - Current status: no unified live performance display was found.
  - Target: show server tick ms, client FPS/frame ms, chunk queue length, rendered chunk count, mob count, item count, packet sizes, and cache sizes.
  - Benefit: makes performance regressions visible without guessing from feel.

---

## Priority 6 - Server / World Performance

- [ ] **Chunk save/load benchmark and optimization pass**
  - Current status: chunk-based saving/loading exists; `server/world/io.py` uses atomic stdlib JSON writes, while `server/world/dyn_chunk_gen.py` has an `orjson` fast path for V3 chunk load/save.
  - Target: benchmark startup, chunk generation, load, save, and autosave on large worlds; decide whether to consolidate chunk I/O on the faster V3 path or keep separate implementations.

- [ ] **Server chunk cache LRU**
  - Current status: client cache eviction exists; server `world_data` may still grow as players explore.
  - Target: server-side max chunk cache with safe reload from disk.

- [ ] **Spatial index for mob separation**
  - Current status: `server/mobs/mob_manager.py` compares every live mob against every other live mob during separation, which is O(mobs^2) per tick.
  - Target: bucket mobs by tile/coarse cell and only compare mobs in the same or neighboring cells.
  - Benefit: keeps mob AI stable as JSON spawn caps increase.

- [ ] **Spatial indexes for mobs, world items, and placed objects**
  - Current status: `server/game_state/game_sync.py` scans all world items and all mobs per player when building nearby state; placed object queries also scan all placed objects.
  - Target: maintain chunk/cell buckets for nearby queries used by game-state sync, pickups, interaction, and rendering payloads.
  - Benefit: reduce repeated O(players * entities) scans to small local lookups.

- [ ] **Avoid building already-sent world chunk payloads**
  - Current status: `server/world/visible.py` builds full visible chunk tile dictionaries before `server/game_state/sync.py` filters against `_sent_chunks`.
  - Target: compute visible chunk keys first, subtract chunks already sent to the player, then gather tile payloads only for unsent chunks.
  - Benefit: repeat world sync ticks become cheap when the player is standing still.
  - Audit note 2026-06-03: `server/world/visible.py` also rebuilds per-call `chunk_to_keys` lists that could be avoided by iterating chunk coordinates directly.

- [ ] **Non-blocking chunk generation CPU sampling**
  - Current status: `server/world/dyn_chunk_gen.py` calls `psutil.cpu_percent(interval=0.05)` inside chunk processing, which can block the game loop for about 50 ms.
  - Target: use `interval=None`, sample less often, or move chunk generation/CPU monitoring to a worker path.

- [ ] **Delta sync for planted nodes**
  - Current status: `server/game_state/game_sync.py` includes the full planted node snapshot in every game-state payload.
  - Target: send initial planted-node snapshot once, then send adds/removes/updates as deltas.

- [ ] **Connection keepalive cleanup**
  - Current status: UDP movement updates `last_seen`; cleanup handles disconnects.
  - Target: periodic timeout cleanup for players with stale `last_seen`, even if sockets remain half-open.

- [ ] **NumPy/Numba fallback strategy**
  - Current status: `server/world/dyn_chunk_gen.py` imports NumPy and Numba at module import.
  - Target: graceful startup message or fallback path if acceleration dependencies are missing.

- [ ] **Dropped world item persistence**
  - Current status: players, placed objects, resource-node depletion/respawn, planted nodes, and chunks persist; dropped `world_items` appear to be runtime-only.
  - Target: persist dropped items separately from chunks so server restarts do not wipe loose drops or dungeon/chest rewards left on the ground.

- [ ] **Serialization/compression benchmark**
  - Current status: the old performance roadmap says zlib compression was done, but current hot paths primarily use `orjson` with length-prefixed messages.
  - Target: benchmark raw `orjson`, zlib-compressed JSON, and optional MessagePack for world chunks/game-state payloads; only adopt compression if it improves real network or CPU behavior.

- [ ] **Human-readable persistence files**
  - Current status: player saves and ops use `indent=2`; placed objects/resource-node persistence/chunk saves mostly do not.
  - Target: decide which files should remain compact for performance and which should use readable formatting.

---

## Documentation / Repo Hygiene

- [ ] **Refresh `README.md`**
  - Current status: partially refreshed on 2026-06-03, but should keep pace with new systems and registry migrations.
  - Target: update feature list, project structure, run instructions, current systems, and screenshots section.

- [ ] **Replace old roadmap docs with this consolidated source of truth**
  - Current status: `todo.md`, `review.md`, `FEATURES_PLAN.md`, and `AUDIT.md` contain overlapping and sometimes stale items.
  - Target: either archive older docs or add a top note pointing readers to `todo_06022026.md`.

- [ ] **Add JSON schema or validation for data files**
  - Current status: `items.json`, `recipes.json`, and mob JSONs are loaded directly.
  - Target: lightweight validation tool for items, recipes, mobs, and any future registries.

- [ ] **Add regression tests for data registries**
  - Target examples: every recipe result exists in `items.json`, every item id referenced by shops/nodes/mobs exists, every mob sprite config has required keys, every placeable item maps both directions.

- [ ] **Keep a live architecture / working-notes file in root**
  - Current status: added `codex_working_notes_06032026.md` as a rolling reference for file responsibilities, active systems, and audit findings.
  - Target: update it during larger refactors so repeated audits do not have to rediscover the same ownership map.

---

## Completed During Verification / Not Carried Forward

- [x] TCP state handler dispatcher exists in `server/network/tcp_state_handlers_v2.py`.
- [x] TCP/UDP player id safety checks exist using `_SAFE_ID`.
- [x] Chest swap slot bounds are checked before `chest_swap()`.
- [x] Block/parry is implemented in client input, UDP player state, `combat.py`, and `mob_manager.py`.
- [x] Slime Lair dungeons and Slime King boss trigger/cooldown exist.
- [x] Towns, NPCs, and NPC shops exist.
- [x] Particle system exists in `client/rendering/particles.py`.
- [x] Minimap exists and persists visited chunks.
- [x] Server loop prints tracebacks and uses self-correcting sleep.
- [x] World chunk sync no longer uses MD5 hashing; `server/game_state/sync.py` tracks sent chunks per player.
- [x] Per-tile biome/elevation metadata exists in current chunk data.
- [x] Alternative message serialization partially landed via `orjson`; MessagePack/compression remains a benchmark item.
- [x] HUD toast queue is capped.
- [x] `rendering.cache.get_item_surface()` exists, though not all callers use it yet.
- [x] Client logout reset no longer references stale mob renderer caches like `_slime`; reset now clears current mob sprite/procedural caches defensively.
